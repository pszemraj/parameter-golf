# 5090 Log

This is the committed logbook for the active RTX 5090 Core/Amplifier work.
Older pre-pivot launcher history was pruned from the live file on 2026-04-26
because it was mostly dead-command context; recover it from git history if
needed.

Current conventions:

- keep entries focused on active code, active protocols, or important cleanup
- include commit hashes after the relevant commit lands
- keep old retired script names out of live planning docs unless the entry is
  explicitly about removing them
- keep raw run artifacts local under `experiments/`, with only interpreted
  results committed here

## 2026-04-26T09:45:00-04:00

- Phase: long-context K4 result read and analyzer hardening
- Completed local reads:
  - `d128_l5_i512` K4 `seq1024` screen: `2.0180296159` bpb at about
    `1.17M` tok/s.
  - `d128_l5_i512` K4 `seq2048` screen: `2.0172393682` bpb at about
    `1.17M` tok/s.
  - `d128_l5_i512` K4 `seq4096` screen: `2.0467157896` bpb; this loses to
    the shorter K4 context screens and should not be promoted.
  - The interrupted/partial `seq512` K4+BPTT2 confirmation reached
    `1.9908271602` bpb at step `6656`, but remains partial evidence only.
- Protocol fix:
  - The geometry analyzer and adaptive planner now resolve the active screen
    contract from flags/run-version hints instead of assuming K2/seq512/BPTT1.
  - Confirmation planning can compare multiple source run versions and emit the
    best context candidate while preserving K/top-k, sequence length, batch
    size, and BPTT chunks in the generated command.
  - Old replaying full-val rows are rejected for confirmation evidence when
    target accounting does not match the validation denominator.
- Current recommendation:
  - Promote `d128_l5_i512` K4 `seq2048` to a clean `1B` full-val confirmation.
  - Keep `seq1024` as backup and kill `seq4096` for now.

## 2026-04-26T08:25:00-04:00

- Phase: adaptive closeout result read
- Completed local results:
  - `d128_l5_i512` K2 is the confirmed geometry winner:
    `2.0031207874` full-val bpb at `1B`, about `1.14M` tok/s.
  - `d96_l6_i512` K2 confirmed worse at `2.0264627708` full-val bpb.
  - `d128_l5_i512` K2 BPTT2 improved the matching `512M` screen by only
    `0.0003` bpb, so BPTT2 is not independently established.
  - `d128_l5_i512` K4+BPTT2 reached `2.0155952297` at `512M`, improving the
    matching K2+BPTT2 screen by about `0.0404` bpb while staying under the
    artifact cap with about `4.7 MB` headroom.
- Next action:
  - run `d128_l5_i512` K4 at `1B` with full validation under the fixed
    exact-tail validation iterator.
  - use BPTT1 for the first K4 confirmation; only run paired K4+BPTT2 if K4 is
    flat/slightly positive and the decision depends on the BPTT interaction.
- Caveat:
  - The completed `1B` full-val rows predate the exact-tail iterator fix. The
    old over-count was small, but final evidence should come from the fixed
    iterator.

## 2026-04-26T08:40:00-04:00

- Commit: this entry is part of the evidence-pipeline hardening commit
- Phase: evidence-pipeline hardening after reviewer audit
- Main observations:
  - The full-validation coverage issue and count-worker propagation were already
    fixed after the reviewed commit.
  - Adaptive planning now distinguishes `commands`, `already_complete`, and
    `blocked`. Blocked stages exit nonzero instead of writing a successful empty
    shell script.
  - The BPTT-to-K4 gate now treats sub-`0.005` bpb changes as noise.
  - `SKIP_DONE` on the maintained Core/Amplifier sweep path now validates the
    resolved run contract before skipping an existing completed directory.
  - Exact BPB setup now records `tokenizer_sha256`, rejects tokenizer/spec vocab
    mismatches, and fails if validation target ids include zero-byte tokenizer
    ids.
  - Periodic checkpoints now store the next step to run, so resume does not
    repeat the just-finished optimizer step.
- Validation:
  - `conda run -s --name train pytest -q tests/test_5090_adaptive_closeout.py tests/test_train_core_amplifier_logging.py tests/test_experiment_utils.py tests/test_5090_geometry_analyzer.py`
  - `conda run -s --name train ruff check` on touched files
  - `conda run -s --name train python -m py_compile tools/plan_5090_adaptive_closeout.py tools/run_core_amp_sweep.py train_core_amplifier.py`
  - blocked planner smoke returned status `blocked` and exit code `2`
  - current K4 planner smoke emits BPTT1 K4 with `--count-workers 4`

## 2026-04-26T08:00:00-04:00

- Commit: `9f76b58`
- Phase: unified trigram-memory spec build cleanup
- Decision:
  - Dense trigram memory is part of the frozen statistical spec, not a separate
    derived model directory.
  - Active 5090 launchers now build one final shared spec via
    `inspect_model.py init --trigram-memory frozen` and train directly from that
    directory.
- Cleanup:
  - Removed the legacy separate-spec trigram launchers and builder tools from the
    maintained surface.
  - The remaining active path is:
    `run_5090_adaptive_closeout.sh` ->
    `run_5090_trigram_aligned_geometry_screen.sh` ->
    `inspect_model.py init`.
- Validation:
  - `bash -n scripts/5090_common.sh scripts/run_5090_final3day_frontier_batch.sh scripts/run_5090_trigram_geometry_matrix.sh scripts/run_5090_trigram_aligned_geometry_screen.sh scripts/run_5090_adaptive_closeout.sh`
  - `conda run -s --name train python -m py_compile inspect_model.py train_core_amplifier.py core_amplifier_lm/config.py core_amplifier_lm/spec_builder.py core_amplifier_lm/trigram_memory.py core_amplifier_lm/__init__.py tools/analyze_5090_geometry_frontier.py tools/plan_5090_adaptive_closeout.py tools/run_core_amp_sweep.py`
  - `conda run -s --name train pytest -q tests/test_trigram_memory.py tests/test_train_core_amplifier_logging.py tests/test_5090_geometry_analyzer.py tests/test_5090_adaptive_closeout.py tests/test_core_amplifier.py -k 'trigram_memory or full_validation or geometry or adaptive or build_spec_optimized_can_return_unified'`

## 2026-04-26T04:28:56-04:00

- Commit: `de95b50`
- Phase: trigram-memory runtime allocation cleanup
- Main observations:
  - Frozen trigram memory was building a full dense `[batch, time, vocab]`
    zero tensor for the sparse K-entry boost, then adding that tensor to the
    already-dense base logits.
  - The model now scatters the trigram residuals directly into the existing
    dense base-logit tensor.
  - This preserves the same literal context boost while removing a large
    per-forward temporary allocation from the maintained K2/K4 path.
- Validation:
  - `conda run -s --name train python -m py_compile core_amplifier_lm/model.py`
  - `conda run -s --name train pytest tests/test_core_amplifier.py -q -k 'trigram_memory_applies_literal_context_boost_and_carries_state or trigram_memory_requires_spec_tensors'`
  - `conda run -s --name train ruff check --fix core_amplifier_lm/model.py`
  - `conda run -s --name train ruff format core_amplifier_lm/model.py`
  - `git diff --check`
- Transformer-regression note:
  - None.
  - This is a statistical-memory runtime cleanup only.

## 2026-04-26T04:31:49-04:00

- Commit: `7b80b4d`
- Phase: Core/Amplifier training hot-path audit cleanup
- Main observations:
  - Delayed dense base-bigram logit lookup until after amplifier/readout work,
    reducing peak live tensors in the maintained trigram-memory forward path.
  - Training no longer calls `.item()` on raw and weighted losses inside every
    microchunk; scalar conversion is deferred to log/eval steps.
  - Hard-loss diagnostic scalars are collected only on the final microchunk of
    a logging step.
  - Validation accumulates loss/byte tensors on device and synchronizes once
    after the eval loop.
  - `accelerated_scan_installed()` is cached so scan backend resolution does
    not repeat import discovery.
- Validation:
  - `conda run -s --name train python -m py_compile core_amplifier_lm/model.py core_amplifier_lm/scan_backend.py train_core_amplifier.py`
  - `conda run -s --name train pytest tests/test_core_amplifier.py -q -k 'trigram_memory_applies_literal_context_boost_and_carries_state or base_bigram_delta or scan_backend'`
  - `conda run -s --name train pytest tests/test_experiment_utils.py -q`
  - `conda run -s --name train ruff check --fix core_amplifier_lm/model.py core_amplifier_lm/scan_backend.py train_core_amplifier.py`
  - `conda run -s --name train ruff format core_amplifier_lm/model.py core_amplifier_lm/scan_backend.py train_core_amplifier.py`
  - `git diff --check`
- Transformer-regression note:
  - None.
  - This is runtime hygiene for the recurrent/statistical-memory path.

## 2026-04-26T04:34:10-04:00

- Commit: `0aaa419`
- Phase: sweep launcher hygiene
- Main observations:
  - `tools/run_core_amp_sweep.py` now accepts typed CLI overrides for the
    active sweep protocol instead of requiring every caller to inject more
    ambient shell variables.
  - Active trigram-memory launchers now pass seed, spec dir, model root,
    W&B grouping, trigram mode, scan backend, and run-spec rows through those
    CLI arguments.
  - Standalone geometry launchers default to `RUN_VERSION=geom1`; top-K
    screen/confirm launchers default to `RUN_VERSION=v2`, matching the current
    docs.
  - The README now documents the boundary: process-local knobs can remain env
    vars; experiment protocol should move through typed CLI/config paths.
- Validation:
  - `conda run -s --name train python -m py_compile tools/run_core_amp_sweep.py`
  - `bash -n` over the then-active geometry and memory launchers
  - `conda run -s --name train python tools/run_core_amp_sweep.py controller --help`
  - `DRY_RUN=1 RUN_BENCHMARK=0 RUN_GEOMETRY_SCREEN=1 bash scripts/run_5090_final3day_frontier_batch.sh`
  - `conda run -s --name train ruff check --fix tools/run_core_amp_sweep.py`
  - `conda run -s --name train ruff format tools/run_core_amp_sweep.py`
  - `git diff --check`
- Transformer-regression note:
  - None.
  - This changes launch protocol plumbing only.

## 2026-04-26T04:35:10-04:00

- Commit: `a0bf386`
- Phase: parallel trigram-memory count preflight
- Main observations:
  - Exact parallel trigram-memory counting uses one dense count table per worker
    plus a parent reduction table.
  - The builder now checks available temp disk and RAM before spawning workers,
    based on the actual dense table footprint.
  - If the requested worker count does not fit, the path fails before partial
    counts or machine-pressure crashes.
- Validation:
  - `conda run -s --name train python -m py_compile core_amplifier_lm/spec_builder.py`
  - `conda run -s --name train pytest tests/test_core_amplifier.py -q -k 'trigram_memory_to_spec or training_token_file_fingerprint'`
  - `conda run -s --name train ruff check --fix core_amplifier_lm/spec_builder.py`
  - `conda run -s --name train ruff format core_amplifier_lm/spec_builder.py`
  - `git diff --check`
- Transformer-regression note:
  - None.
  - This is build-time resource validation for the statistical-memory spec.

## 2026-04-26T04:37:48-04:00

- Commit: `887ea96`
- Phase: trainer allocation and loss overhead cleanup
- Main observations:
  - Random, directory, and sequential stream batchers now reuse host batch
    buffers where that is safe, avoiding repeated batch tensor allocation in
    the direct-shard path.
  - Reused pinned host buffers use a synchronous device copy to avoid modifying
    the pinned source before an asynchronous copy finishes.
  - Per-log approximate train BPB uses the byte-count mean computed once at
    setup instead of synchronizing a GPU mean on every log.
  - `cross_entropy_per_token()` no longer forces an explicit dense fp32 logits
    copy when CUDA autocast is active; non-autocast behavior remains fp32.
- Validation:
  - `conda run -s --name train python -m py_compile train_core_amplifier.py`
  - CPU smoke for random/direct/sequential batchers and per-token CE
  - `conda run -s --name train pytest tests/test_experiment_utils.py -q`
  - `conda run -s --name train ruff check --fix train_core_amplifier.py`
  - `conda run -s --name train ruff format train_core_amplifier.py`
  - `git diff --check`
- Transformer-regression note:
  - None.
  - This is trainer runtime cleanup for the maintained recurrent path.

## 2026-04-26T04:38:30-04:00

- Commit: `b2ec025`
- Phase: optional watch-metric synchronization cleanup
- Main observations:
  - Branch-router entropy and token-gate mean/std no longer call `.item()` in
    the model forward pass.
  - The values stay as detached tensors and convert to Python floats only when
    `latest_watch_metrics()` is called by the logging path.
  - This keeps optional gate/router probes from forcing per-forward GPU syncs
    if those modes are re-enabled.
- Validation:
  - `conda run -s --name train python -m py_compile core_amplifier_lm/model.py`
  - `conda run -s --name train pytest tests/test_core_amplifier.py -q -k 'residual_token_gate or branch_router or trigram_memory_applies_literal_context_boost_and_carries_state'`
  - `conda run -s --name train ruff check --fix core_amplifier_lm/model.py`
  - `conda run -s --name train ruff format core_amplifier_lm/model.py`
  - `git diff --check`
- Transformer-regression note:
  - None.
  - This affects optional controller diagnostics only.

## 2026-04-26T04:50:22-04:00

- Commit: `a5f0532`
- Phase: sweep protocol CLI migration
- Main observations:
  - `tools/run_core_amp_sweep.py` now exposes typed CLI flags for active
    protocol/runtime fields: data path/dtype, optimizer constants, eval/log/save
    cadence, full-val final, core/controller options, trigram memory, W&B,
    compile, gradient checkpointing, mmap/autocast, and token-device movement.
  - Active trigram-memory launchers pass those fields through CLI flags instead
    of relying on the sweep runner preset or ambient environment.
  - Serious launchers now explicitly route `--no-compile` and
    `--no-gradient-checkpointing` to the sweep runner, so those settings cannot
    silently revert to preset defaults.
  - Environment variables remain only for process-local controls and shell
    entrypoint overrides.
- Validation:
  - `conda run -s --name train python -m py_compile tools/run_core_amp_sweep.py`
  - `bash -n` over the then-active 5090 launchers
  - `conda run -s --name train ruff check --fix tools/run_core_amp_sweep.py`
  - `conda run -s --name train ruff format tools/run_core_amp_sweep.py`
  - `conda run -s --name train python tools/run_core_amp_sweep.py controller --help`
  - `DRY_RUN=1 RUN_BENCHMARK=0 RUN_GEOMETRY_SCREEN=1 bash scripts/run_5090_final3day_frontier_batch.sh`
  - dry-run checks for the then-active memory screen and confirmation launchers
  - short real GPU smoke through `tools/run_core_amp_sweep.py` using explicit
    `--no-compile --no-gradient-checkpointing --no-wandb`
  - `conda run -s --name train pytest tests/test_experiment_utils.py -q`
  - `git diff --check`
- Transformer-regression note:
  - None.
  - This is experiment-protocol plumbing only.

## 2026-04-26T03:56:00-04:00

- Commit: `0cd76d8`
- Phase: obsolete queue-helper removal
- Removed:
  - `tools/run_core_amp_blocks01_confirm1b.py`
  - `tools/run_core_amp_fullspec_reruns.py`
  - `tools/run_core_amp_schedule_sweeps.py`
  - matching queue-only tests
  - stale point-in-time notes under `notes/`
- Main observations:
  - These helpers encoded completed pre-trigram-memory rerun queues and were no
    longer part of the three-day closeout protocol.
  - Their only live references were their own tests and old log history.
  - Removing them cuts duplicate-report noise and avoids re-surfacing obsolete
    blocked/schedule/fullspec lanes as plausible next actions.
- Transformer-regression note:
  - None.
  - This removes orchestration and notes only; it does not alter the active
    model path.

## 2026-04-26T03:55:00-04:00

- Commit: `d4ee28a`
- Phase: historical baseline archive removal
- Removed:
  - root transformer baseline copy
  - root MLX baseline copy
  - historical `records/` archive
- Main observations:
  - The duplicate report was dominated by archived transformer submissions and
    W&B snapshots, not maintained Core/Amplifier code.
  - The active branch should be read as a Core/Amplifier experiment repo, not as
    an upstream leaderboard archive.
  - Historical leaderboard/example material remains recoverable through git.
- Transformer-regression note:
  - Positive cleanup.
  - Removing archived transformer baselines reduces accidental drift back toward
    the transformer-shaped solutions this branch is trying to avoid.

## 2026-04-26T03:39:05-04:00

- Commit: `3b2fb2c`
- Phase: obsolete 5090 launcher removal after trigram-memory pivot
- Main observations:
  - Removed completed or stopped pre-pivot launcher scripts.
  - Current maintained execution is now the trigram-memory geometry/top-K path.
- Follow-up docs stamp:
  - Commit `a7a6c0d` pruned active docs so they point at the current launcher
    surface.
- Transformer-regression note:
  - None.
  - This removed stale orchestration only.

## 2026-04-26T03:36:00-04:00

- Commit: `5340173`
- Phase: trigram memory table-cache backfill
- Main observations:
  - If a matching output memory spec already exists but the reusable counted
    table cache is missing, the builder now backfills the table cache from the
    existing `spec.pt` before returning.
  - This prevents compatible geometry shapes from repeatedly doing the full CPU
    trigram count when one spec was already built but the shared table cache was
    absent or deleted.
- Validation:
  - capped memory-spec builder smoke at `--max-tokens 200000`
    confirmed existing-spec table-cache backfill after deleting the cached
    table file.
- Transformer-regression note:
  - None.
  - This is cache behavior only.

## 2026-04-26T03:20:31-04:00

- Commit: `1f51662`
- Phase: final-week protocol naming and cache-safety cleanup
- Main observations:
  - Trigram tables are named as spec-resident memory, not as an external add-on.
  - The final `spec.pt` contains the trigram memory tensors; the separate table
    cache is only a reusable counted-tensor build artifact for compatible specs.
  - The memory spec cache and counted-table cache include a training-shard
    fingerprint so partial or changed datasets do not silently reuse stale
    statistics.
  - Serious launchers print both the memory-spec cache root and the counted
    table cache root so a full-data recount is distinguishable from training.
  - Legacy `trigram_sidecar` config keys are accepted only for old-run
    analysis; new configs and launchers write `trigram_memory`.
- Follow-up docs stamp:
  - Commit `7285ee6` recorded validation.
- Transformer-regression note:
  - None.
  - This is terminology, cache protocol, and launcher clarity only.

## 2026-04-26T02:49:16-04:00

- Commit: `eb74efc`
- Phase: reviewer-aligned final-three-day geometry frontier
- Main observations:
  - The prior `128x4x4` framing was too shallow: it is a speed-frontier point,
    not the aligned answer.
  - The shape frontier is expressed as `core_dim x layers x inner_dim` so
    `inner_dim=512` is explicit and fractional expansion cannot silently
    truncate to `511`.
  - The default Stage 1 rows are:
    - `blocks0_d96_l6_i512`
    - `blocks0_d64_l10_i512`
    - `blocks0_d128_l4_i512`
    - `blocks0_d128_l5_i512`
  - The batch script intentionally stops after benchmark plus fixed-token
    geometry screens; confirmations require an analysis pass.
- Transformer-regression note:
  - None.
  - This remains frozen high-order context memory plus a recurrent controller,
    not attention or token-token transformer compute.

## 2026-04-26T03:56:30-04:00

- Commit: `f6a0b7e`
- Phase: duplicate-report resave and live-doc cleanup
- Local scratch outputs:
  - `local-scratch/dupes_tracked_python.json`
  - `local-scratch/tracked_python_files.json`
- Main observations:
  - The huge duplicate report is now filtered down to tracked Python files only.
  - After archive and obsolete-queue pruning, the tracked report contains:
    - `27` tracked Python files
    - `12` hybrid duplicate pairs
    - `9` semantic duplicate pairs
    - `10` traditional duplicate pairs
  - Remaining duplicate pressure is mostly test similarity plus small helper
    pairs, not obsolete launch protocols.
- Next action:
  - continue with the active geometry/top-K result analysis after the current
    user-run batch finishes
- Validation:
  - `bash -n` over the then-active 5090 and sweep launchers
  - `conda run -s --name train python -m py_compile train_gpt.py inspect_model.py train_core_amplifier.py core_amplifier_lm/*.py tools/*.py tests/*.py`
  - `conda run -s --name train ruff check --fix core_amplifier_lm train_core_amplifier.py inspect_model.py tools tests train_gpt.py data/download_hf_docs_and_tokenize.py`
  - `conda run -s --name train ruff format core_amplifier_lm train_core_amplifier.py inspect_model.py tools tests train_gpt.py data/download_hf_docs_and_tokenize.py`
  - `conda run -s --name train pytest -q`
  - `DRY_RUN=1 FRONTIER_BATCH_ID=geom1 RUN_VERSION=geom1 SEEDS=1337 RUN_BENCHMARK=0 bash scripts/run_5090_final3day_frontier_batch.sh`
  - `git diff --check`

## 2026-04-26T04:24:00-04:00

- Commit: `f528444`
- Phase: exact parallel trigram-memory counting option
- Main observations:
  - The dense trigram-memory builder was serial over files/chunks; full K2/K4
    counts spent most of their time in one Python process running
    `np.unique(..., return_counts=True)`.
  - Added `TRIGRAM_COUNT_WORKERS` / `--count-workers` for exact worker-local
    dense count tables over contiguous shard blocks.
  - This avoids unsafe shared-table writes. Workers count privately, then the
    parent reduces the dense tables.
  - The mode is memory/disk hungry: at SP1024, each worker needs about
    `4.29 GB` for its local count table before reduction.
  - Default remains `1` worker so running jobs and low-memory launches are not
    silently changed.
- Transformer-regression note:
  - None.
  - This is systems plumbing for the same frozen high-order context memory.
- Validation:
  - `bash -n` over the then-active 5090 launchers
  - `conda run -s --name train python -m py_compile core_amplifier_lm/spec_builder.py` plus the then-active memory builder
  - `conda run -s --name train pytest tests/test_core_amplifier.py -q -k 'trigram_memory_to_spec or training_token_file_fingerprint'`
  - `DRY_RUN=1 TRIGRAM_COUNT_WORKERS=4 RUN_VERSION=workers_smoke SEEDS=1337 RUN_BENCHMARK=0 RUN_GEOMETRY_SCREEN=1 bash scripts/run_5090_final3day_frontier_batch.sh`
  - `conda run -s --name train ruff check --fix core_amplifier_lm/spec_builder.py tests/test_core_amplifier.py` plus the then-active memory builder
  - `conda run -s --name train ruff format core_amplifier_lm/spec_builder.py tests/test_core_amplifier.py` plus the then-active memory builder
  - `git diff --check`

## 2026-04-26T05:09:28-04:00

- Commit: this entry is part of the hardening commit
- Phase: reviewer hardening for final-three-day frontier reruns
- Main observations:
  - Active 5090 launchers now accept flag-first experiment protocol for the
    geometry frontier, trigram-memory screen, and 1B confirmation paths.
  - `run_5090_trigram_geometry_matrix.sh` now passes explicit geometry flags
    into the aligned launcher and hard-forces `--geometry-num-blocks 0` for the
    default frontier rows.
  - Cached trigram-memory specs are no longer accepted by shell existence
    checks alone; the builder is invoked so metadata validation runs before
    reuse.
  - The geometry analyzer now treats non-completed rows as pending, validates
    the screen contract before emitting commands, hydrates old summaries from
    `resolved_config.json`, and prints flag-based confirmation commands.
  - Summary rows now expose `core_dim`, `core_inner_dim`, and
    `recurrent_cells` directly.
- Current geom1 read:
  - `blocks0_d96_l6_i512`: completed, `val_bpb=2.0668155804`, delta
    `-0.008356`, benchmark speed ratio `1.713`, decision `promote_1b`.
  - Other default frontier rows are still pending/missing locally.
- Transformer-regression note:
  - None.
  - This hardening protects the frozen trigram-memory plus recurrent-controller
    path; it does not add attention or transformer-style token-token compute.
- Validation:
  - `bash -n` over the then-active 5090 launchers
  - `conda run -s --name train python -m py_compile core_amplifier_lm/experiment.py tools/analyze_5090_geometry_frontier.py` plus the then-active memory builder
  - `conda run -s --name train pytest tests/test_5090_geometry_analyzer.py tests/test_experiment_utils.py -q` plus the then-active memory-builder tests
  - `bash scripts/run_5090_final3day_frontier_batch.sh --dry-run --no-run-benchmark --run-version geom_cli_smoke --seeds 1337 --trigram-top-k 2 --count-workers 2`
  - dry-run checks for the then-active memory screen and confirmation launchers
  - `conda run -s --name train python tools/analyze_5090_geometry_frontier.py --run-version geom1 --benchmark logs/5090_final3day/geom1/geometry_frontier_benchmark.json`

## 2026-04-26T05:34:00-04:00

- Commit: this entry is part of the subagent verification follow-up
- Phase: dry-run and command-surface cleanup
- Main observations:
  - The review hardening was mostly present, but dry-run launchers still reached
    `tools/run_core_amp_sweep.py --dry-run`, which imports the training stack.
  - Active dry-runs now print the would-run `inspect_model.py`,
    memory-builder, and `run_core_amp_sweep.py` commands and
    return before invoking trainer or trigram-cache resolver Python modules.
  - Current user-facing docs now show flag-first commands for the geometry batch
    and top-K memory screen instead of ambient protocol variables.
  - Trigram table-cache reuse now compares embedded cache metadata against the
    requested top-K/smoothing/clip/cap/data fingerprint before attaching tensors.
  - Docs now state explicitly that current K2 trigram memory is additive with one
    global learned scale, not yet controller-arbitrated by confidence features.
- Transformer-regression note:
  - None.
  - This is launcher hygiene around the same frozen-memory recurrent path.
- Validation:
  - `bash -n` over the then-active 5090 launchers
  - `conda run -s --name train python -m py_compile tools/analyze_5090_geometry_frontier.py core_amplifier_lm/experiment.py` plus the then-active memory builder
  - `conda run -s --name train pytest tests/test_5090_geometry_analyzer.py tests/test_experiment_utils.py -q` plus the then-active memory-builder tests
  - `conda run -s --name train ruff check --fix` over the then-active memory builder and tests
  - `conda run -s --name train ruff format` over the then-active memory builder and tests
  - `bash scripts/run_5090_final3day_frontier_batch.sh --dry-run --no-run-benchmark --run-version geom_cli_smoke --seeds 1337 --trigram-top-k 2 --count-workers 2`
  - dry-run checks for the then-active memory screen and confirmation launchers
  - `conda run -s --name train python tools/analyze_5090_geometry_frontier.py --run-version geom1 --benchmark logs/5090_final3day/geom1/geometry_frontier_benchmark.json`
  - docstring check over the then-active memory builder
  - `git diff --check`

## 2026-04-26T05:50:00-04:00

- Commit: this entry is part of the adaptive closeout runner commit
- Phase: bounded adaptive experiment orchestration
- Main observations:
  - Added `tools/plan_5090_adaptive_closeout.py`, which reads completed summaries and
    selects only the next useful command for each stage.
  - Added `scripts/run_5090_adaptive_closeout.sh`, which runs:
    geometry frontier -> selected confirmations -> best-confirmed BPTT2 ->
    one K4 screen on the selected best geometry/BPTT setting.
  - The default cap is `--max-confirmations 2`, so the adaptive path does not
    explode into a geometry x BPTT x K Cartesian product.
  - Dry-run executes nested dry-runs and writes generated plan scripts under
    `logs/5090_adaptive_closeout/<id>/` without launching training commands.
- Transformer-regression note:
  - None.
  - The orchestration keeps the frozen trigram-memory recurrent path and only
    changes which bounded ablation stage is selected next.
- Validation:
  - `bash -n scripts/run_5090_adaptive_closeout.sh scripts/run_5090_final3day_frontier_batch.sh scripts/run_5090_trigram_aligned_geometry_screen.sh`
  - `conda run -s --name train python -m py_compile tools/plan_5090_adaptive_closeout.py`
  - `conda run -s --name train pytest tests/test_5090_adaptive_closeout.py tests/test_5090_geometry_analyzer.py -q`
  - `conda run -s --name train ruff check --fix tools/plan_5090_adaptive_closeout.py tests/test_5090_adaptive_closeout.py`
  - `conda run -s --name train ruff format tools/plan_5090_adaptive_closeout.py tests/test_5090_adaptive_closeout.py`
  - `bash scripts/run_5090_adaptive_closeout.sh --dry-run --run-version geom1 --seed 1337 --frontier-batch-id geom1 --no-run-benchmark --count-workers 2 --max-confirmations 2 --stop-after k4`
  - `conda run -s --name train python ~/scripts/py/doc_check.py tools/plan_5090_adaptive_closeout.py --check-lazy-docstrings`
  - `git diff --check`

## 2026-04-26T06:20:00-04:00

- Commit: this entry is part of the adaptive closeout smoke hardening commit
- Phase: tiny end-to-end smoke validation
- Main observations:
  - Added an explicit `--smoke-test` path to the adaptive closeout runner and
    active geometry launchers.
  - Smoke mode uses one geometry, tiny step counts, tiny batch/sequence sizes,
    capped trigram/data tokens, and W&B disabled.
  - The planner now accepts an explicit contract for screen, confirmation, and
    variant stages, so the same adaptive code can validate small smoke runs
    without weakening the serious default contract.
  - Real smoke `smoke_live1` completed:
    frontier K2 screen -> selected confirmation -> BPTT2 -> K4+BPTT2.
  - A second dry-run against `smoke_live1` selected no commands, confirming
    completed-stage skip checks work.
- Transformer-regression note:
  - None.
  - This only hardens orchestration around the frozen trigram-memory recurrent
    path.
- Validation:
  - `bash -n scripts/run_5090_adaptive_closeout.sh scripts/run_5090_final3day_frontier_batch.sh scripts/run_5090_trigram_geometry_matrix.sh scripts/run_5090_trigram_aligned_geometry_screen.sh`
  - `conda run -s --name train python -m py_compile tools/plan_5090_adaptive_closeout.py`
  - `conda run -s --name train pytest tests/test_5090_adaptive_closeout.py tests/test_5090_geometry_analyzer.py -q`
  - `bash scripts/run_5090_adaptive_closeout.sh --smoke-test --dry-run --run-id smoke_contract --no-run-benchmark --stop-after k4 --count-workers 1`
  - `bash scripts/run_5090_adaptive_closeout.sh --smoke-test --run-id smoke_live1 --no-run-benchmark --stop-after k4 --count-workers 1`
  - `bash scripts/run_5090_adaptive_closeout.sh --smoke-test --dry-run --run-id smoke_live1 --no-run-benchmark --stop-after k4 --count-workers 1`
  - `conda run -s --name train ruff check --fix tools/plan_5090_adaptive_closeout.py tests/test_5090_adaptive_closeout.py`
  - `conda run -s --name train ruff format tools/plan_5090_adaptive_closeout.py tests/test_5090_adaptive_closeout.py`
  - `conda run -s --name train python ~/scripts/py/doc_check.py tools/plan_5090_adaptive_closeout.py --check-lazy-docstrings`
  - `git diff --check`

## 2026-04-26T09:40:00-04:00

- Commit: this entry is part of the evidence-slop hardening follow-up
- Phase: post-closeout evidence and cache hardening
- Main observations:
  - Completed local geometry/adaptive rows now put `blocks0_d128_l5_i512` in
    front: K2 1B full-final-val screen `2.0031207874` BPB at about
    `1.14M tok/s`.
  - K4+BPTT2 512M completed at `2.0155952297` BPB and remains well under the
    artifact cap: `11,318,493` estimated bytes, `4,681,507` bytes headroom.
  - BPTT2 alone was noise-level positive on `d128_l5_i512`
    (`0.000322` BPB), so the planner now selects BPTT1 for the next K4 screen
    unless the BPTT2 gain clears the explicit `0.005` BPB threshold.
  - `SKIP_DONE` now refuses to reuse completed runs whose resolved contract
    changed and also refuses full-final-validation skips when the last eval did
    not actually report full coverage.
  - Directory mmap caches are now keyed by shard contract instead of fixed
    `train_int32.bin` / `val_int32.bin` names, so capped smoke caches cannot
    poison full runs.
  - The aligned geometry launcher runs an artifact preflight after shared-spec
    build and before training, using a conservative trainable-payload estimate
    and failing if the projected artifact exceeds the 16 MB cap.
  - Generic `run_core_amp_sweep.py controller` / structure init paths now expose
    trigram top-K/count-worker/cache protocol flags instead of relying only on
    ambient variables.
  - Summary rows now surface trigram-memory identity fields and tokenizer SHA so
    run TSVs are more auditable without opening nested JSON.
- Transformer-regression note:
  - None.
  - The changes are evidence and orchestration guardrails around the same
    frozen trigram-memory plus recurrent-controller architecture.
- Validation:
  - `bash -n scripts/run_5090_trigram_aligned_geometry_screen.sh`
  - `conda run -s --name train python -m py_compile tools/run_core_amp_sweep.py tools/plan_5090_adaptive_closeout.py train_core_amplifier.py tools/estimate_artifact_bytes.py core_amplifier_lm/experiment.py`
  - `conda run -s --name train pytest -q tests/test_5090_adaptive_closeout.py tests/test_experiment_utils.py tests/test_core_amplifier.py::test_mmap_train_val_requires_explicit_val_shard tests/test_core_amplifier.py::test_mmap_train_val_cache_is_keyed_by_contract tests/test_train_core_amplifier_logging.py`
  - `conda run -s --name train ruff format ...` over touched Python files
  - `conda run -s --name train ruff check ...` over touched Python files
  - `conda run -s --name train python ~/scripts/py/doc_check.py ... --check-lazy-docstrings` over touched Python files
  - `bash scripts/run_5090_trigram_aligned_geometry_screen.sh --smoke-test --dry-run --run-version smoke_preflight --seeds 1337 --geometry-label blocks0_d96_l6_i512 --geometry-core-dim 96 --geometry-core-layers 6 --geometry-core-inner-dim 512 --num-steps 2 --lr-hold-steps 1 --batch-size 2 --seq-len 64 --target-effective-step-tokens 128 --val-steps 1 --trigram-max-tokens 200000 --data-max-tokens 131072 --count-workers 2 --no-wandb`
  - `conda run -s --name train python tools/estimate_artifact_bytes.py experiments/5090_architecture/blocks0_d128_l5_i512_trigram_seed1337_geom1_k4_bptt2/blocks0_d128_l5_i512_trigramk4_lr0035_h3500_512m_bptt2_k4_s1337 --fail-over-limit`
  - `conda run -s --name train python tools/analyze_5090_geometry_frontier.py --run-version geom1 --benchmark logs/5090_final3day/geom1/geometry_frontier_benchmark.json --baseline-tok-s 586729`
  - `conda run -s --name train python tools/plan_5090_adaptive_closeout.py --stage k4 --run-version geom1 --seed 1337 --benchmark logs/5090_final3day/geom1/geometry_frontier_benchmark.json --count-workers 4 --emit markdown`
  - `git diff --check`

## 2026-04-26T10:25:00-04:00

- Commit: this entry is part of the W&B project correction commit
- Phase: W&B project hygiene
- Main observations:
  - Active 5090 launchers were overriding the trainer's default W&B project
    with a stale hybrid-convolution ablation project default.
  - The maintained Core/Amplifier branch default is now `pg-core-amp` in the
    launcher, serious-run guard, README, and 5090 planning docs.
  - Added a regression test so the retired project name cannot silently return
    in the maintained launcher/docs surface.
  - Also updated local gitignored `AGENTS.md`; it is intentionally not part of
    the commit.
- Transformer-regression note:
  - None.
  - This is experiment bookkeeping only.
- Validation:
  - `bash -n scripts/5090_common.sh scripts/run_5090_trigram_aligned_geometry_screen.sh`
  - `conda run -s --name train pytest -q tests/test_5090_wandb_project.py tests/test_experiment_utils.py::test_run_core_amp_sweep_cli_exposes_trigram_protocol`
  - `conda run -s --name train ruff format tests/test_5090_wandb_project.py`
  - `conda run -s --name train ruff check tests/test_5090_wandb_project.py`
  - `bash scripts/run_5090_trigram_aligned_geometry_screen.sh --smoke-test --dry-run --run-version wandb_project_check --seeds 1337 --geometry-label blocks0_d96_l6_i512 --geometry-core-dim 96 --geometry-core-layers 6 --geometry-core-inner-dim 512 --num-steps 2 --lr-hold-steps 1 --batch-size 2 --seq-len 64 --target-effective-step-tokens 128 --val-steps 1 --trigram-max-tokens 200000 --data-max-tokens 131072 --count-workers 1 --no-wandb`
  - `git diff --check`

## 2026-04-26T16:00:00-04:00

- Commit: this entry is part of the exact-BPB sentinel-target fix
- Phase: confirmation preflight hardening
- Main observations:
  - The K4+BPTT2 confirmation failed before training because exact BPB setup
    rejected the validation shard's zero-byte `<s>` target ids.
  - Exact byte-normalized BPB now skips approved tokenizer control-token
    targets for the BPB numerator and denominator while preserving token loss
    over all validation targets.
  - Unexpected zero-byte target ids still fail loudly.
  - Run results and summary TSVs now record positive-byte target count,
    skipped zero-byte target count, and skipped zero-byte target ids.
  - The real explicit validation shard contains `49,999` zero-byte targets,
    all id `1`, out of `62,021,845` validation targets.
- Transformer-regression note:
  - None.
  - This is metric accounting for the same recurrent/statistical model path.
- Validation:
  - `conda run -s --name train python -m py_compile train_core_amplifier.py`
  - `conda run -s --name train pytest -q tests/test_train_core_amplifier_logging.py`
  - Direct no-W&B tokenizer/validation-shard accounting check against
    `data/datasets/fineweb10B_sp1024/fineweb_val_000000.bin`
  - No-W&B trainer startup smoke using the failed K4 confirmation config/spec
    copied to a temp directory with `--num-steps 0 --val-steps 0 --force-device cpu`
  - `conda run -s --name train ruff format train_core_amplifier.py core_amplifier_lm/experiment.py tests/test_train_core_amplifier_logging.py`
  - `conda run -s --name train ruff check train_core_amplifier.py core_amplifier_lm/experiment.py tests/test_train_core_amplifier_logging.py`
  - `conda run -s --name train python ~/scripts/py/doc_check.py train_core_amplifier.py --check-lazy-docstrings`
  - `conda run -s --name train python ~/scripts/py/doc_check.py core_amplifier_lm/experiment.py --check-lazy-docstrings`
  - `git diff --check`

## 2026-04-26T16:53:00-04:00

- Commit: this entry is part of the validation-console cleanup
- Phase: 5090 run-output hygiene
- Main observations:
  - Sampled validation is now reported once at startup as
    `sampled_eval_targets=<sampled>/<available>` with `val_steps`, `seq_len`,
    eval batch size, source, and final-full-val flag.
  - Periodic eval console lines now print only `val_loss` and `val_bpb`.
  - Final eval still prints the actual final eval sample/full-validation target
    count.
  - Existing JSON/summary coverage fields are retained for analyzer contract
    checks, but the terminal no longer frames sampled validation as a coverage
    percentage.
- Transformer-regression note:
  - None.
  - This is logging-only.
- Validation:
  - `conda run -s --name train python -m py_compile train_core_amplifier.py`
  - `conda run -s --name train pytest -q tests/test_train_core_amplifier_logging.py`
  - `conda run -s --name train ruff format train_core_amplifier.py tests/test_train_core_amplifier_logging.py`
  - `conda run -s --name train ruff check train_core_amplifier.py tests/test_train_core_amplifier_logging.py`
  - `conda run -s --name train python ~/scripts/py/doc_check.py train_core_amplifier.py --check-lazy-docstrings`
  - `git diff --check`
